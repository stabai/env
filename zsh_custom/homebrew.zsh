# Note: You may have to add this command in .zshrc before sourcing oh-my-zsh.sh
# script. Any plugins that rely on applications installed through Homebrew won't
# work properly otherwise. But it doesn't matter if this command runs multiple
# times, so you can still leave this here.

eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
